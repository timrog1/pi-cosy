"use strict";
// var gpio = require("rpi-gpio");
// var pin = 12;
// var value = false;
// // setInterval(() =>
	// gpio.setup(pin, gpio.DIR_OUT, () => { 
			// gpio.write(pin, value, () => {});
			// value = !value;
		// }), 1000);